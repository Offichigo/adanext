# Base de donnees (profil prod uniquement)
# En dev, H2 est utilise automatiquement -- ces variables ne sont pas necessaires
DATABASE_URL=jdbc:postgresql://localhost:5432/adanext
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=changeme

# Port du serveur (optionnel, defaut : 8080)
PORT=8080
